# QuickJS-NG Compiler and Linker Flags

Return the flags needed for compiling against the bundled quickjs-ng
headers and library

## Usage

``` r
cppflags()

ldflags()
```

## Value

A character string of flags
